package contest;

public class CountIntegers {
	public int count(String num1, String num2, int min_sum, int max_sum) {
		if(max_sum >= Integer.parseInt(num2))
			return Integer.parseInt(num2) - Integer.parseInt(num1) + 1;
		int count = 0;
		// String nums = "0123456789";
		
		return count;
		
	}
}
